export default StreamLogger;
export class StreamLogger extends Stream.Transform {
    constructor(direction: any, verbose?: boolean);
    _direction: any;
    _verbose: boolean;
    _transform(data: any, encoding: any, onData: any): void;
    _log(data: any): void;
}
export namespace StreamLogger {
    let SEND: number;
    let RECEIVE: number;
}
import Stream from 'stream';
//# sourceMappingURL=stream-logger.d.ts.map
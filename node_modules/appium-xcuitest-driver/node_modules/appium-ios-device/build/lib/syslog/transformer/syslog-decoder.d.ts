export default SyslogDecoder;
export class SyslogDecoder extends Stream.Transform {
    constructor(bufferLength: any);
    bufferIndex: number;
    buffer: Buffer;
    _transform(data: any, encoding: any, onData: any): void;
    _decode(data: any): void;
}
/**
 * Unescapes utf8-encoded characters, so the output looks
 * like a valid string. See https://github.com/appium/appium/issues/14476
 * for more details.
 *
 * @param {Buffer} logBuffer A single escaped log line
 * @returns {string} The unescaped string or the same string if no unescaping
 * is necessary.
 */
export function toUtf8String(logBuffer: Buffer): string;
import Stream from 'stream';
//# sourceMappingURL=syslog-decoder.d.ts.map
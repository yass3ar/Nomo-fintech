"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PlistServiceEncoder = void 0;
const stream_1 = __importDefault(require("stream"));
const support_1 = require("@appium/support");
const HEADER_LENGTH = 4;
class PlistServiceEncoder extends stream_1.default.Transform {
    constructor() {
        super({ objectMode: true });
    }
    _transform(data, encoding, onData) {
        this.push(this._encode(data), 'binary');
        onData();
    }
    _encode(data) {
        const payloadBuffer = support_1.plist.createBinaryPlist(data);
        const headerBuffer = Buffer.alloc(HEADER_LENGTH);
        headerBuffer.writeUInt32BE(payloadBuffer.length, 0);
        return Buffer.concat([headerBuffer, payloadBuffer], headerBuffer.length + payloadBuffer.length);
    }
}
exports.PlistServiceEncoder = PlistServiceEncoder;
exports.default = PlistServiceEncoder;
//# sourceMappingURL=plist-service-encoder.js.map
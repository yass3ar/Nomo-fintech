"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PlistServiceDecoder = void 0;
const lodash_1 = __importDefault(require("lodash"));
const stream_1 = __importDefault(require("stream"));
const support_1 = require("@appium/support");
const HEADER_LENGTH = 4;
class PlistServiceDecoder extends stream_1.default.Transform {
    constructor() {
        super({ objectMode: true });
    }
    _transform(data, encoding, onData) {
        this._decode(data);
        onData();
    }
    _decode(data) {
        const payload = data.slice(HEADER_LENGTH, data.length);
        if (lodash_1.default.isEmpty(payload)) {
            return;
        }
        const object = support_1.plist.parsePlist(payload);
        if (!object) {
            return;
        }
        this.push(object);
    }
}
exports.PlistServiceDecoder = PlistServiceDecoder;
exports.default = PlistServiceDecoder;
//# sourceMappingURL=plist-service-decoder.js.map